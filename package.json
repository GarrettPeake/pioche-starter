{
  "name": "pioche-starter",
  "author": "Garrett Peake",
  "version": "1.0.0",
  "description": "Starter repository for pioche projects",
  "type": "module",
  "main": "src/entry.ts",
  "scripts": {
    "build": "pioche build",
    "dev": "pioche build && wrangler dev --local",
    "serve": "pioche build && wrangler dev --local",
    "deploy": "pioche build && wrangler publish"
  },
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "wrangler": "^2.0.2"
  },
  "dependencies": {
    "pioche": "^0.7.8"
  }
}
