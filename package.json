{
  "name": "pioche-starter",
  "version": "1.0.0",
  "type": "module",
  "main": "src/entry.ts",
  "scripts": {
    "build": "pioche-scripts build",
    "dev": "pioche-scripts build && wrangler dev --local",
    "serve": "pioche-scripts build && wrangler dev --local",
    "deploy": "pioche-scripts build && wrangler publish"
  },
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "pioche-scripts": "^0.1.0",
    "wrangler": "^2.0.2"
  },
  "dependencies": {
    "pioche": "^0.8.0"
  }
}
